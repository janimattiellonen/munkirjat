<?php if(isset($this->authors) ):

    $str = '';
    
    foreach($this->authors as $author):
        $str .= $autho->getFullname() . ', ';
    endforeach;
    
    $str = rtrim($st, ', ');

    echo $str;
    
endif;?>